{
  "name": "chicken-or-pasta",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-router-dom": "^4.3.1",
    "react-scripts": "^2.1.1"
  },
  "scripts": {
    "start": "export NODE_PATH=src/ && react-scripts start",
    "build": "export NODE_PATH=src/ &&react-scripts build",
    "test": "export NODE_PATH=src/ &&react-scripts test --env=jsdom",
    "eject": "export NODE_PATH=src/ &&react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
